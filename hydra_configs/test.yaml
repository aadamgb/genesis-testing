defaults:
  - task: hover
  - override hydra/job_logging: disabled
  - override hydra/hydra_logging: disabled        
  - _self_

e: drone-hovering
c: 300
t: 20
record: false
export: false
     
log_dir: ???  # required by user!

hydra:
  run:
    dir: ${log_dir}
  job:
    chdir: false       
  output_subdir: null   